<%- include('partials/head') %>
<%- include('partials/nav', { user }) %>

<main class="min-h-[70vh] flex items-center justify-center px-6">
  <div class="w-full max-w-lg card p-8">
    <h2 class="text-2xl font-bold mb-4">Upgrade to Pro</h2>
    <p class="muted mb-6">Unlimited summaries, priority processing and saved history export.</p>

    <div class="mb-4 p-4 rounded" style="background: rgba(250,207,85,0.08); border:1px solid rgba(250,207,85,0.12);">
      <div class="flex items-center justify-between">
        <div>
          <div class="font-semibold">Pro Plan</div>
          <div class="muted text-sm">Monthly</div>
        </div>
        <div class="text-2xl font-bold">₹99</div>
      </div>
    </div>

    <button id="payBtn" class="w-full py-3 rounded-lg btn-accent">Pay with Razorpay</button>
    <p class="text-xs muted mt-3">Test keys in development. Production uses Razorpay.</p>
  </div>
</main>

<%- include('partials/footer') %>
<%- include('partials/scripts') %>
